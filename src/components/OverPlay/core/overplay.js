const overplay = {
  toggle: () => {
    // setVisible((prev) => !prev);
  },

  active: () => {
    // setVisible(true);
  },

  disable: () => {
    // setVisible(false);
  },
};

export default overplay;
